[project]
name = "cognicore-memory"
version = "1.0.0"
description = "Persistent local memory for Claude Desktop powered by CogniCore"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
